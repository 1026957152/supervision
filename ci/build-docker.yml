---
platform: linux

image_resource:
  type: docker-image
  source: {repository: docker:dind}


inputs:
  - name: repo
  - name: build_jar
#inputs:
#  - name: build
#run:
#  path: ls
run:
  path: sh
  args: ["repo/ci/build-docker.sh"]
#  args: [hello world]