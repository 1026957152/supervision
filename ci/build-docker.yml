---
platform: linux

image_resource:
  type: docker-image
  source: {repository: docker:dind}


inputs:
  - name: repo
#inputs:
#  - name: build
#run:
#  path: ls
run:
  path: /bin/sh
  args: ["repo/ci/build-docker.sh"]
#  args: [hello world]